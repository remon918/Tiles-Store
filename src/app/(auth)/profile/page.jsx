import React from 'react';

export const metadata = {
  title: "Your DBL Profile",
};

const ProfilePage = () => {
    return (
        <div>
            Your Profile Here
        </div>
    );
};

export default ProfilePage;